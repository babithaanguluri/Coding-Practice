# Jagged Array Program in Java

This project demonstrates how to create and use a **Jagged Array** in java.
A jagged array is an array of arraya where each inner array can have a **different length**.
## 📌What This Program Does
- Creates a jagged array
- Each row has a different number of columns
- Stores and prints the elements using nested loops
- Shows how dynamic memory allocation works for 2D uneven arrays

## 🛠️Technologies Used
- Java
- IDE: VS Code
- Git & Github for version control
